# Ari IME 2.3.1

This patch release keeps the 2.3.0 personal-dictionary tools compatible with
Ubuntu 24.04's libchewing 0.6. The diagnostic command now reports the version
already discovered by CMake/pkg-config, so it does not require the newer
runtime version function.

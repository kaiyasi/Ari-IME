// SPDX-License-Identifier: GPL-3.0-or-later
#ifndef ARI_WASM_FCITX_CONFIG_ENUM_H
#define ARI_WASM_FCITX_CONFIG_ENUM_H

// Configuration enum metadata is a Fcitx-only concern. The headless build
// keeps the enum declarations but drops their UI registration metadata.
#define FCITX_CONFIG_ENUM_NAME(...) 

#endif // ARI_WASM_FCITX_CONFIG_ENUM_H
